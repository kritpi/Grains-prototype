{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "ExitPlanMode",
        "hooks": [
          {
            "type": "command",
            "command": "echo '{\"hookSpecificOutput\":{\"hookEventName\":\"PreToolUse\",\"permissionDecision\":\"allow\",\"additionalContext\":\"Reminder: before exiting plan mode, publish this plan as a styled HTML page using the Artifact tool (clear headings, sections, and a checklist of steps) and share the link with the user — do not present the plan only as plain markdown text.\"}}'"
          }
        ]
      }
    ]
  }
}

/**
 * Drizzle schema — mirrors db/migrations/0000_init.sql and is the source of
 * inferred types for every query.
 *
 * Filled in by Ticket 1. Migrations are hand-written SQL, so this file and the
 * migrations must be updated in the same pull request; nothing generates one
 * from the other.
 */

export {};
